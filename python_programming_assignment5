#1. Write a Python Program to Find LCM?
def lcm(x,y):
    if x > y:
        greater = x
    else:
        greater = y
    while(True):
        if (greater % x == 0) and (greater % y == 0):
            lcm = greater
            break
        greater += 1
    return lcm
    
    
 #2. Write a Python Program to Find HCF?
def hcf(x,y):
    if x > y :
        smaller = y 
    else:
        smaller = x 
    for i in range(1,smaller+1):
        if (x % i == 0) and (y % i == 0):
            hcf = i
    return hcf
    
 

#3. Write a Python Program to Convert Decimal to Binary, Octal and Hexadecimal?

x = int(input("Enter a no."))
print(f"The no in binary is {bin(x)}")
print(f"The no in octal is {oct(x)}")
print(f"The no in hexadecimal is {hex(x)}")


#4. Write a Python Program To Find ASCII value of a character?
x = int(input("Enter a no."))
print(f"The ASCII value of no. is {ascii(x)}")



#5. Write a Python Program to Make a Simple Calculator with 4 basic mathematical operations?

x= int(input("Enter a no."))
y= int(input("Enter another no."))
print("Select 1 for addition, 2 for subtraction, 3 for multiplication and 4 for division")
z= int(input("Enter your choice from 1,2,3,4 "))
if z == 1:
    print(x+y)
elif z == 2:
    print(x-y)
elif z == 3 :
    print(x*y)
elif z== 4 :
    print(x/y)
else:
    print("Selection out of range")
    
